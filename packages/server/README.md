# @vibecc/paykit-server

Hono routes (checkout, webhooks, billing, admin), DB schema, repos, and provider clients (SePay + Stripe). Mounts via `createPaykit().routes()`.

Phases 03-07 will populate this package.

## License

Proprietary.
